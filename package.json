{
  "name": "vinyllib",
  "version": "1.0.0",
  "description": "monorepo for vinyllib",
  "scripts": {
    "api": "cd ./api && npm run dev",
    "frontend": "cd ./frontend && npm run start",
    "start": "concurrently --kill-others \"npm run api\" \"npm run frontend\""
  },
  "author": "mpleines",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "react-outside-click-handler": "^1.3.0"
  }
}
